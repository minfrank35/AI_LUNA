import 'package:flutter/cupertino.dart';

class CommRowItem extends StatelessWidget {
  const CommRowItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
